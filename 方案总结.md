# 痤疮严重程度分级 —— 分类知识蒸馏方案总结

## 项目目标

对痤疮面部图像进行 **4 级严重程度分级**（Grade 0/1/2/3），输出等级标签及置信度概率。

---

## 整体架构

```
┌─────────────────────────┐    软标签蒸馏     ┌──────────────────────────┐
│  Module 1: 教师模型      │ ──────────────→ │  Module 3: 学生模型 (KD)  │
│  ResNet-50 + LDL        │   KL divergence  │  ResNet-34/18 + DKD       │
│  84.59% (fold_1)        │                  │  最高 84.93%               │
└─────────────────────────┘                  └──────────┬───────────────┘
                                                       │ 加载权重
                                                       ▼
                                            ┌──────────────────────────┐
                                            │  Module 4: 推理 & 集成    │
                                            │  AcneGrader / Ensemble   │
                                            │  单张 → 分级 + 概率       │
                                            └──────────────────────────┘
```

### 为什么用知识蒸馏而不是直接分类

教师 ResNet-50 使用 LDL（Label Distribution Learning，标签分布学习）训练，输出端是一个"软"的概率分布，包含了等级之间的相似关系（如 Grade 1 和 Grade 2 比 Grade 1 和 Grade 3 更相似）。KD 让学生从这些软标签中学习，而不仅仅是硬标签的 4 分类，传递了更丰富的知识。

### 为什么不走 CAM + 分割路线

CAM 激活图仅能定位"最具类别区分度的区域"，不具备逐实例分割能力。从 2-bit 分类标签反推 50K+ 像素的精细分割图存在信息瓶颈，Otsu 阈值化的单团块伪掩码无法刻画痤疮密集分布、形态多变的特征，形成弱监督死循环。详见 `CAM路线问题分析.docx`。

---

## Module 1：教师模型

| 项目 | 说明 |
|---|---|
| 架构 | ResNet-50（ImageNet 预训练，BN 冻结） |
| 训练方法 | LDL + dual-sigma（标签分布学习） |
| 损失函数 | KL(预测分布, 高斯平滑标签) |
| 数据增强 | Resize(256) → RandomCrop(224) → RandomHorizontalFlip → Normalize |
| 优化器 | SGD(lr=0.001, momentum=0.9, weight_decay=5e-4) |
| 学习率策略 | MultiStepLR([30,45], gamma=0.5) |
| 训练轮数 | 60 epochs |
| 5 折交叉验证 | fold_1 best: **84.59%** |

---

## Module 3：分类知识蒸馏

### 核心方法：DKD (Decoupled Knowledge Distillation)

将 KD 损失拆分为两部分，防止目标类抑制非目标类的暗知识：

- **TCKD（Target Class KD）**：目标类的二元蒸馏，`BCE(s_target, t_target)`
- **NCKD（Non-target Class KD）**：非目标类的 KL 蒸馏，`KL(s_non || t_non)`

```
L_total = α_kd × (α_dkd × L_tckd + (1-α_dkd) × L_nckd) + (1-α_kd) × L_ce
```

超参：α_kd=0.7, α_dkd=0.5, T=3.0

### 学生模型

| 模型 | 参数量 | 教师参数占比 | 推理速度 | 用途 |
|---|---|---|---|---|
| ResNet-18 | 11.2M | 47.5% | 快 2.4× | 轻量部署 |
| **ResNet-34** | **21.3M** | **90.6%** | 快 1.5× | **主力推荐** |

### 训练配置

| 项目 | ResNet-18 版 | ResNet-34 版（最终） |
|---|---|---|
| 数据增强 | Resize→Crop→Flip→ToTensor→Rotate→Norm | 同左 |
| 类别平衡 | 无 | **WeightedRandomSampler** |
| 优化器 | SGD(lr=0.001) | SGD(lr=0.001) |
| 学习率策略 | MultiStepLR([30,45]) | **CosineAnnealingLR(T=90)** |
| 训练轮数 | 60 epochs | **90 epochs** |
| KD 方法 | DKD | DKD |

### 训练流程

1. 加载教师 ResNet-50 权重（mode/fold_X_best.pth），冻结所有参数
2. 初始化学生 ResNet-18/34（ImageNet 预训练，BN 冻结）
3. 预计算：教师对整个训练集做一次前向推理，得到每个样本的 4 维软标签概率
4. 训练循环：每个 batch 从预计算表中取教师 probs，计算 DKD loss + CE loss
5. 每 epoch 在测试集评估：Accuracy、混淆矩阵、per-class 指标
6. 保存最佳模型权重

### 实验结果

| 方案 | 总体准确率 | Class 0 | Class 1 | Class 2 | Class 3 |
|---|---|---|---|---|---|
| 教师 ResNet-50 + LDL | 84.59% | 91.3% | 80.3% | 77.8% | 88.5% |
| ResNet-18 + DKD (60ep) | 82.19% | 86.4% | 88.2% | 52.8% | 76.9% |
| **ResNet-34 + WS + Cos (90ep)** | **84.93%** | **88.3%** | **84.3%** | **72.2%** | **92.3%** |

**最终方案超越教师 0.34 个百分点，同时参数量减少约 10%。**

### 关键改进点

1. **WeightedRandomSampler**：Class 2/3 的训练曝光量提升 3-4 倍，Class 2 从 52.8% → 72.2%（+19.4%）
2. **ResNet-34 换 ResNet-18**：更大容量（21M vs 11M），更好地容纳教师传递的知识
3. **CosineAnnealingLR**：替代阶梯式衰减，让模型在低 lr 区间精细收敛，最后 30 个 epoch 仍有提升
4. **DKD 解耦蒸馏**：独立处理目标类/非目标类，防止 TCKD 抑制 NCKD 的暗知识

---

## Module 4：推理部署

### 文件结构

```
module4/
├── __init__.py        # 模块声明
├── inference.py        # 单模型推理（AcneGrader）
└── ensemble.py         # 多折集成推理（EnsembleGrader）
```

### 使用方式

**单模型推理：**

```python
from module4.inference import AcneGrader, grade_to_label

grader = AcneGrader('module3_output_kd/xxx/fold_1_best.pth')
grade, probs = grader.predict('patient_image.jpg')

print(f'Grade: {grade} ({grade_to_label(grade)})')
# Grade: 2 (Severe (Grade 2))
print(probs)
# {'grade_0': 0.032, 'grade_1': 0.118, 'grade_2': 0.614, 'grade_3': 0.236}
```

**5 折集成推理（更稳定）：**

```python
from module4.ensemble import EnsembleGrader

checkpoints = [
    'output/fold_0_best.pth',
    'output/fold_1_best.pth',
    'output/fold_2_best.pth',
    'output/fold_3_best.pth',
    'output/fold_4_best.pth',
]
grader = EnsembleGrader(checkpoints)
grade, avg_probs, fold_details = grader.predict('patient_image.jpg')
# 5 个模型各自预测，取平均 softmax 概率后 argmax
```

**命令行：**

```bash
# 单模型
python module4/inference.py --checkpoint <模型路径> --image <图片路径>

# 集成
python module4/ensemble.py --checkpoints fold_0.pth fold_1.pth ... --image <图片路径>
```

---

## 文件总览

```
code/
├── mode/                              # 教师模型权重
│   ├── fold_1_best.pth               # fold_1 教师（84.59%）
│   └── fold_1.jsonl                  # 教师训练日志
│
├── module3_kd/                        # 分类知识蒸馏模块
│   ├── __init__.py
│   ├── config_kd.py                  # 训练配置（超参、路径、增强参数）
│   ├── student_model.py              # 学生模型（ResNet-18 / ResNet-34）
│   ├── kd_losses.py                  # 损失函数（Vanilla KD + DKD）
│   ├── dataset_kd.py                 # 数据集封装（复用 DatasetProcessing）
│   ├── train_kd.py                   # 训练主脚本（含 CLI）
│   └── evaluate_kd.py                # 评估脚本
│
├── module3_output_kd/                 # KD 训练输出
│   └── 20260528_194233/              # ResNet-34 最佳结果
│       ├── fold_1_best.pth           # 学生模型（84.93%）
│       ├── fold_1.jsonl              # 训练日志
│       └── run_config.json           # 运行配置记录
│
├── module4/                           # 最终推理模块
│   ├── __init__.py
│   ├── inference.py                  # 单模型推理（AcneGrader）
│   └── ensemble.py                   # 5 折集成推理（EnsembleGrader）
│
├── data/ACNE04/Classification/       # 数据集
│   ├── JPEGImages/                   # 图像文件
│   ├── NNEW_trainval_0~4.txt        # 5 折训练集标注
│   └── NNEW_test_0~4.txt            # 5 折测试集标注
│
├── 方案总结.md                        # 本文件
└── CAM路线问题分析.docx               # CAM 路线不可行分析
```

---

## 复现步骤

```bash
# 1. 训练单个 fold
python module3_kd/train_kd.py --folds "1" --epochs 90

# 2. 训练全部 5 折（用于集成）
python module3_kd/train_kd.py --folds "0,1,2,3,4" --epochs 90

# 3. 单模型推理
python module4/inference.py --checkpoint module3_output_kd/xxx/fold_1_best.pth --image <图片路径>

# 4. 5 折集成推理
python module4/ensemble.py --checkpoints fold_0.pth fold_1.pth fold_2.pth fold_3.pth fold_4.pth --image <图片路径>
```
